name: Docker Check

on:
  pull_request:
    branches:
      - develop
      - main
    paths:
      - .github/workflows/docker-check.yml
      - backend/Dockerfile
      - frontend/Dockerfile
      - docker-compose.yml
      - docker-compose.yaml
      - backend/requirements.txt
      - frontend/package.json
      - frontend/package-lock.json
      - mosquitto/**

permissions:
  contents: read

concurrency:
  group: docker-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  docker-build:
    name: Docker — Compose Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect compose file
        id: compose_check
        run: |
          if [ -f docker-compose.yml ]; then
            echo "file=docker-compose.yml" >> "$GITHUB_OUTPUT"
          elif [ -f docker-compose.yaml ]; then
            echo "file=docker-compose.yaml" >> "$GITHUB_OUTPUT"
          else
            echo "file=" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip until Docker Compose exists
        if: steps.compose_check.outputs.file == ''
        run: |
          echo "::warning::Docker Compose is not initialized yet. Skipping Docker build check."

      - name: Validate Docker Compose config
        if: steps.compose_check.outputs.file != ''
        env:
          MYSQL_ROOT_PASSWORD: ci_mysql_root_password
          MYSQL_DATABASE: manttoai_db
          SECRET_KEY: ci_secret_key_for_compose_validation
          MQTT_USERNAME: ci_mqtt_user
          MQTT_PASSWORD: ci_mqtt_password
          SEED_ADMIN_PASSWORD: ci_admin_password
        run: |
          bash scripts/setup_env.sh
          docker compose -f "${{ steps.compose_check.outputs.file }}" config --quiet

      - name: Build services
        if: steps.compose_check.outputs.file != ''
        run: docker compose -f "${{ steps.compose_check.outputs.file }}" build
